// Default hero image pair - same source for both sides
export const DEFAULT_HERO: { originalUrl: string; resultUrl?: string | null } = {
  originalUrl: "/hero/portrait-3840.jpg",
  resultUrl: "/hero/portrait-3840.jpg", // same source; right side is unblurred
}
